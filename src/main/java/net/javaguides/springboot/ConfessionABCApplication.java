package net.javaguides.springboot;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ConfessionABCApplication {

	//Springboot is a Java web framework
	//Springboot can create stand alone application that runs on its own without relying one external web server
	public static void main(String[] args) {
		SpringApplication.run(ConfessionABCApplication.class, args);
		
	}

}
